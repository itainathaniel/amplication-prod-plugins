"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.appendImports = void 0;
/**
 * This function will append an import to the imports of a AST source file by searching for the last import and inserting the new import after it.
 * This file mutate the file ast object.
 */
function appendImports(file, imports) {
    const { program: { body }, } = file;
    let locationOfLastImport = 0;
    body.forEach((node, i) => {
        if (node.type === "ImportDeclaration") {
            locationOfLastImport = i;
        }
    });
    const newImportLocation = locationOfLastImport === 0 ? 0 : locationOfLastImport + 1;
    body.splice(newImportLocation, 0, ...imports);
    return file;
}
exports.appendImports = appendImports;
//# sourceMappingURL=appendImports.js.map