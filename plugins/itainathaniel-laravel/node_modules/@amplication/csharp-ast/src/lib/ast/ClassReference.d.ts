import { AstNode } from "../core/AstNode";
import { Writer } from "../core/Writer";
export declare namespace ClassReference {
    interface Args {
        name: string;
        namespace: string;
    }
}
export declare class ClassReference extends AstNode {
    readonly name: string;
    readonly namespace: string;
    constructor({ name, namespace }: ClassReference.Args);
    write(writer: Writer): void;
}
export declare const OneOfClassReference: ClassReference;
export declare const StringEnumClassReference: ClassReference;
