import { ClassReference } from "../..";
import { AstNode } from "./AstNode";
export declare namespace Writer {
    interface Args {
        namespace?: string;
    }
}
export declare class Writer {
    private buffer;
    private indentLevel;
    private hasWrittenAnything;
    private lastCharacterIsNewline;
    private references;
    private namespace;
    constructor({ namespace }: Writer.Args);
    write(text: string): void;
    writeNode(node: AstNode): void;
    writeLine(text?: string): void;
    newLine(): void;
    writeNewLineIfLastLineNot(): void;
    indent(): void;
    dedent(): void;
    addReference(reference: ClassReference): void;
    toString(): string;
    /*******************************
     * Helper Methods
     *******************************/
    private writeInternal;
    private isAtStartOfLine;
    private getIndentString;
    private stringifyImports;
}
